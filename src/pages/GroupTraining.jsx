// GroupTraining - schedule and run group training sessions with shared notes,
// dictation, and an AI meeting summary. Placeholder shell; the build fleet
// fleshes this out against training.js group APIs. NO em-dash / en-dash.
import React from 'react';
import { SectionHeader } from '../components/UI.jsx';

export default function GroupTraining() {
  return (
    <div className="fade-up">
      <SectionHeader title="Group training" sub="Run a session for the team, capture notes and dictation, get a summary. Coming online." />
    </div>
  );
}
